# Headers are managed in netlify.toml
# This file is kept minimal to avoid conflicts with netlify.toml definitions.
# netlify.toml takes precedence when both define the same header for the same path.
